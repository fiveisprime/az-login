{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "node",
            "request": "launch",
            "name": "Explicit",
            "program": "${workspaceRoot}/examples/explicit.js",
            "protocol": "inspector",
            "env": {
                "azureServicePrincipalClientId": ""
            }
        }
    ]
}